% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters.R
\name{add_filter}
\alias{add_filter}
\title{Add a New Filter Definition}
\usage{
add_filter(
  id,
  title,
  target,
  condition,
  character_only = FALSE,
  overwrite = FALSE
)
}
\arguments{
\item{id}{\code{character} The id of this filter. Only upper case letters and
numbers are allowed, e.g. \code{"LARGECAP"}.}

\item{title}{\code{character} The title of the filter}

\item{target}{\code{character} The target dataset of the filter, e.g.
\code{"TICKERS"} or \code{"PRICES"}}

\item{condition}{The filter condition}

\item{character_only}{\code{logical}. Is \code{condition} a string? Defaults to
\code{FALSE}.}

\item{overwrite}{\code{logical} Should existing filters be overwritten? Defaults
to \code{FALSE}.}
}
\value{
A \code{list} of \code{title}, \code{target} and \code{condition}, invisibly
}
\description{
Add a new filter definition or overwrite an existing one. A filter binds an
unevaluated \code{condition} to a \code{target} dataset so that it can be applied
later by \code{\link[=apply_filter]{apply_filter()}}. Unless \code{character_only = TRUE}, \code{condition} is
captured with \code{\link[=substitute]{substitute()}} and therefore written without quotes.
}
\examples{
add_filter(
  id = "LARGECAP",
  title = "Large Cap",
  target = "TICKERS",
  condition = MARKET_CAP >= 1e10
)

add_filter(
  id = "TECH",
  title = "Technology Sector",
  target = "TICKERS",
  condition = "SECTOR == 'Technology'",
  character_only = TRUE
)

}
